@page "/changepassword"

<PageTitle>Change Password</PageTitle>

<PhasePlaceholder Title="Change Password"
                  Summary="The change-password route now exists in Blazor. Auth dependencies, contract verification, and form behavior are deferred to Phase 2."
                  RoutePath="/changepassword"
                  SourceFiles="ChangePasswordSourceFiles"
                  DeferredItems="ChangePasswordDeferredItems" />

@code {
    private static readonly string[] ChangePasswordSourceFiles =
    [
        "src/pages/ChangePassword.jsx",
        "src/context/AuthContext.jsx"
    ];

    private static readonly string[] ChangePasswordDeferredItems =
    [
        "Verify the existing request contract in Phase 2.",
        "Implement auth-aware access behavior in Phase 2.",
        "Restore form validation and error messaging in Phase 2."
    ];
}